type Query {
  bookById(id: ID): Book
  authorByName(authorInput: AuthorInput): Author
}

input AuthorInput {
  firstName: String
  lastName: String!
}

type Book {
  id: ID!
  name: String!
  pageCount: Int
  authors: [Author]!
}

type Author {
  id: ID!
  firstName: String
  lastName: String!
}
